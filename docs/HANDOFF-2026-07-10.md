# Session Handoff — 2026-07-10 (final, supersedes the copy on main)

## State of the world

**SSM deploy-config migration: DONE for dev/test/staging, live and verified.** Deploy config (CloudFront distribution ID, Cognito IDs) flows Terraform → SSM `/birdz/<env>/deploy/*` → deploy jobs. No PAT anywhere. `AWS_ROLE_ARN` (per env) and `SUBMODULE_SSH_KEY` are the only manual secrets. Each env's `https://<env>.birdz.3569081.xyz/config.json` serves real values. Main is at `4258cc2`.

**Prod: NOT migrated.** `prod` branch is still at `bdbbbc1` (pre-migration). The remote session's permissions could not push prod.

**Branch `claude/session-context-ANx5g` (this branch) holds finished-but-undeployed work** — merging it to main auto-applies terraform to dev via the Terraform workflow (a prior remote session lacked permission to push this merge):
- `4a711aa` — **DynamoDB persistence infra** (backlog P1), per `docs/persistence-design.md` Option A: Cognito identity pool per env (federated from the existing user pool), authenticated IAM role restricted by `dynamodb:LeadingKeys` (row-scoped), `game-data` table (PK `userId`, SK `recordKey`, on-demand, PITR), and `identity-pool-id` / `game-data-table` added to the deploy-config SSM params; `config.json` gains `identityPoolId`, `gameDataTable`, `region`. Modules validate.
- `8dc571c` — **Audio hosting design** (`docs/audio-hosting-design.md`): serve bird calls from the existing site buckets under `audio/`; deploy syncs now carry `--exclude "audio/*"` so uploads survive deploys. No new infra needed; blocker is sourcing licensed audio + app playback wiring.

## To do, in order

1. **Merge this branch → main.** Watch the Terraform (dev apply: identity pool, role, table, 2 SSM params) and Deploy workflows. Race note: if deploy-dev fetches SSM before terraform-dev creates the two new params, deploy fails — re-run Deploy after terraform is green. Verify `https://dev.birdz.3569081.xyz/config.json` gains `identityPoolId`/`gameDataTable`.
2. **Promote test and staging**: merge main into each branch, push, watch, verify config.json (same race note per env).
3. **Promote prod** (merge main → prod, push): terraform-prod applies the SSM migration AND the persistence infra together, forgets prod's old GitHub secrets; deploy-prod waits for manual approval. Verify `https://birdz.3569081.xyz/config.json`.
4. **Secret cleanup** (GitHub settings, after prod): delete expired repo secret `PAT`; delete unused `CF_DISTRIBUTION_ID`/`COGNITO_USER_POOL_ID`/`COGNITO_CLIENT_ID` env secrets. KEEP `AWS_ROLE_ARN` + `SUBMODULE_SSH_KEY`. Then a cleanup commit: drop `github` from `required_providers` and the `removed` blocks in all four env main.tfs.
5. **App backlog (needs the birdzReact repo in session scope; none of it is started):**
   - **P0 mobile layout** — UI not optimized for small screens.
   - **P0 biome detection** — Nominatim + Overpass with no retry, silent heuristic fallback, Overpass 504s in production.
   - **P1 Cognito integration** — replace localStorage auth using `config.json`'s `userPoolId`/`clientId` (SRP flow; a pre-sign-up Lambda auto-confirms users, no email verification).
   - **P1 DynamoDB integration** — per `docs/persistence-design.md` step 4: exchange the user-pool session for identity-pool credentials, key items by the Cognito **identity ID**, `Query(userId)` on login, item writes during play, localStorage as offline fallback.
   - **P1 audio playback** — per `docs/audio-hosting-design.md` step 4.
   - P2s: hint-cost display on reveal, silent error handling (persistence.ts / biomeDetector.ts / exifExtractor.ts), Leaflet CDN fallback, password validation, Vitest coverage.

## Environment gotchas (for a Claude session)

- birdzReact is a **private submodule**; CI checks it out over SSH with the `SUBMODULE_SSH_KEY` deploy key. A sandbox without credentials cannot `git submodule update` — work on birdzReact needs that repo added to the session's sources.
- The old session's hourly auto-resume Routine ("Birdz backlog auto-resume") may still be attached to the previous session — delete it if it's still firing.
- Pushes to `main`/`test`/`staging`/`prod` trigger deploys; terraform applies are `-auto-approve` on push when `terraform/**` changed.
