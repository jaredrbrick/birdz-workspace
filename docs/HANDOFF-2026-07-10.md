# Session Handoff — 2026-07-10

## Shipped today

- **SSM deploy-config migration complete** (continuation of HANDOFF-2026-07-09's in-progress work):
  - Mirrored dev's transform to `terraform/environments/{test,staging,prod}/main.tf`: `module.deploy_config` writes `/birdz/<env>/deploy/{cf-distribution-id,cognito-user-pool-id,cognito-client-id}` to SSM; github provider block and the four `github_actions_environment_secret` resources removed; `removed` blocks forget (not destroy) the old secrets.
  - `deploy.yml`: all four deploy jobs now assume the OIDC role **first**, fetch config from SSM, then write `config.json`, sync, and invalidate. `CF_DISTRIBUTION_ID` / `COGNITO_*` secret references deleted; `AWS_ROLE_ARN` remains (OIDC bootstrap).
  - `terraform.yml`: the four `GITHUB_TOKEN: ${{ secrets.PAT }}` lines are gone. **No PAT is used anywhere anymore.**
- **Rolled out and verified**:
  - `main`/dev: terraform apply green (3 SSM params created, deploy-role policy updated, 4 secrets forgotten with the expected warning), deploy green, `https://dev.birdz.3569081.xyz/config.json` serves real pool/client IDs.
  - `test`: promoted (fast-forward to `cc10420`), terraform + deploy green, config.json verified.
  - `staging`: promoted, terraform + deploy green, config.json verified.
- Docs updated: `cicd.md` Step 3 rewritten for the SSM model; backlog rows for AWS hosting/CloudFront marked done.

## Remaining manual steps (user)

1. **Promote prod**: push/merge `main` → `prod` branch (the remote session's permission policy blocked Claude from pushing prod). `terraform-prod` applies immediately (creates prod SSM params, forgets prod GitHub secrets); `deploy-prod` pauses for your manual approval in GitHub. After approval, verify `https://birdz.3569081.xyz/config.json`.
2. **Delete dead secrets** in GitHub settings (needs repo admin): the expired repository secret `PAT`, and the now-unused environment secrets `CF_DISTRIBUTION_ID`, `COGNITO_USER_POOL_ID`, `COGNITO_CLIENT_ID` in all environments. Keep `AWS_ROLE_ARN` (all envs) and `SUBMODULE_SSH_KEY`.
3. **Cleanup commit** (after prod's forget-apply has run): drop `github` from `required_providers` in all four env main.tfs.

## Backlog state / next up

- **DynamoDB persistence (P1)**: terraform module + design doc drafted on branch `claude/session-context-ANx5g` (`terraform/modules/game-data`, `docs/persistence-design.md`) — a proposal for review, not wired into any environment.
- **App-code items** (P0 mobile layout, P1 audio playback, P0 biome detection) live in the **birdzReact** repo, which this workspace session could not access (submodule needs credentials; repo not in session scope). Start a session with birdzReact added as a source to work on those.

## How to resume

Open Claude Code in this repo and say: *"Resume from docs/HANDOFF-2026-07-10.md"*.
